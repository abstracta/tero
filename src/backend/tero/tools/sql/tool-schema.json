{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "dbType": {
      "type": "string",
      "enum": ["azure"],
      "default": "azure",
      "description": "Database type"
    },
    "server": {
      "type": "string",
      "description": "SQL Server fully qualified domain name (e.g. myserver.database.windows.net)"
    },
    "azureTenantId": {
      "type": "string",
      "description": "Azure Active Directory Tenant ID",
      "dependencies": { "dbType": { "const": "azure" } }
    },
    "azureClientId": {
      "type": "string",
      "description": "Azure AD Application (Client) ID. The app registration must list the redirect URI {frontend_url}/tools/sql/oauth-callback and have the Azure SQL Database user_impersonation permission.",
      "dependencies": { "dbType": { "const": "azure" } }
    },
    "azureClientSecret": {
      "type": "string",
      "writeOnly": true,
      "description": "Client secret of your Azure AD app registration.",
      "dependencies": { "dbType": { "const": "azure" } }
    }
  },
  "required": ["dbType", "server", "azureTenantId", "azureClientId", "azureClientSecret"],
  "additionalProperties": false
}
